@using Domain
@{
    var lTelas = (CTelas)ViewBag.aTela;
    var aCampo = ViewBag.aCampo as string;
}
<div class="modal-content card">
    <div class="modal-header card-header">
        <h4 class="modal-title" id="TestarLabel">Adicionar @lTelas.Help</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    </div>
    <div id="sadicionar" class="modal-body card-body">
        @{ Html.RenderPartial("_cAdicionar"); }
    </div>
    <div class="modal-footer card-footer">
    </div>
</div>